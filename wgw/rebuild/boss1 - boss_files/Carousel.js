var swiperConfig = {
  width: 300,                     // 容器宽度（最小300, 默认800）
  height: 300,                    // 容器高度（最小120, 默认300）
  speed: 3000,                    // 切换速度
  needPrevAndNextBtnGroup: true, // 是否需要前进后退按钮组
  needPagination: true           // 是否需要分页器
}